services:
  postgres:
    image: postgres:17-alpine
    container_name: predictus-postgres
    environment:
      POSTGRES_USER: predictus
      POSTGRES_PASSWORD: predictus
      POSTGRES_DB: predictus
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U predictus"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data: